<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Edit a Destination</title>
</head>
<body>
  <%- include('../partials/_nav') %>
  <main>
  <h1>Edit a new Destination</h1>
  <form action="/destinations/<%= destination._id %>?_method=PUT" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" id="name" value="<%= destination.name %>"/>

    <label for="country">Country:</label>
    <input type="text" name="country" id="country" value="<%= destination.country %>"/>

    <label for="description">Description:</label>
    <textarea name="description" id="description"><%= destination.description %></textarea>

    <label for="population">Population:</label>
    <input type="number" name="population" id="population" value="<%= destination.population %>"/>

    <label for="isPopular">Is Popular?:</label>
    <input type="checkbox" name="isPopular" id="isPopular" <% if(destinations.isPopular){ %> checked <% } %>/>

    <button class="btn" type="submit">Edit Planet</button>
  </form>
  <a class="link" href="/planets">Back to Planets</a>
  </main>
</body>
</html>